RESTIC_REPOSITORY={{ item.repository }}
RESTIC_PASSWORD={{ item.password }}
RESTIC_CACHE_DIR=/var/lib/restic/{{ item.name }}
{% for key, value in item.credentials.items() %}
{{ key | upper }}={{ value }}
{% endfor %}
