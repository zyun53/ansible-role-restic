[Unit]
Description=Timer for {{ item.name }}
Requires=restic-{{ item.name }}.service

[Timer]
Unit=restic-{{ item.name }}.service
{% for cl in item.calendars %}
OnCalendar={{ cl }}
{% endfor %}

[Install]
WantedBy=timers.target
